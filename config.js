// Configuración de la app — editable sin reconstruir.
//
// Por default apunta al backend de producción en Render. Para correr el
// frontend contra un backend local, comentar la línea de abajo (queda
// http://localhost:8001/api por defecto en api.js).
window.API_BASE_URL = 'https://playa-backend.onrender.com/api';
