// Configuracion de la app — editable sin reconstruir.
// En local dejar comentado (default: http://localhost:8001/api).
// En Render, setear el URL del backend desplegado.
// window.API_BASE_URL = 'https://playa-backend.onrender.com/api';
